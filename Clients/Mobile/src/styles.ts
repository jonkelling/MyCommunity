import { StyleSheet, TextStyle } from "react-native";

export const text = StyleSheet.create({
    baseText: {
        fontFamily: "Cochin",
    } as TextStyle,
    titleText: {
        fontSize: 20,
        fontWeight: "bold",
        alignSelf: "center",
    } as TextStyle,
});
