import React from "react";
import { ViewProperties } from "react-native";
// tslint:disable-next-line:no-var-requires
const Shoutem = require("@shoutem/ui");

const Overlay = (props) => <Shoutem.Overlay {...props} />;

export default Overlay;
